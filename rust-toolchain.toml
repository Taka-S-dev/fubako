# CI と手元で同じコンパイラを使うための固定。これが無いと CI は毎回その時点の
# 最新 stable を引くため、コードを一行も変えていないのに Rust が上がった日に
# 新しい lint で落ちる（2026-08-27 に chunks_exact_to_as_chunks で発生した）。
# 上げるときはここを書き換え、clippy の新しい指摘に手元で対処してから push する
[toolchain]
channel = "1.98.0"
components = ["clippy", "rustfmt"]
